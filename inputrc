# Case-insensitive tab completion
set completion-ignore-case on

# Show all completions immediately without "display all X possibilities?" prompt
set completion-query-items -1

# Show completions on first Tab instead of second
set show-all-if-ambiguous on
set show-all-if-unmodified on

# Append slash to completed directory names
set mark-directories on
set mark-symlinked-directories on

# Colour completions by file type
set colored-stats on
set colored-completion-prefix on

# Arrow keys for history search
"\e[A": history-search-backward
"\e[B": history-search-forward
